RNP Studio 6.7 — Consolidated Index, No Missing Mural Asset

This package does NOT require assets/rnp-galaxy-mind.jpg.

The duplicate-arm Bruce Lee mural references remain removed.
The affected surfaces now use a built-in cinematic CSS background, so nothing can render blank
because of a missing JPG.

Replace:
- index.html
- service-worker.js
- manifest.webmanifest
- package.json

import {allow,generateJson,fail,compact} from "./_lib/openai.js";
export default async function handler(req,res){if(!allow(req,res))return;try{const body=req.body||{};const result=await generateJson({instructions:`You are the RNP Producer's Table. Protect the artist's truthful seed, never invent life events, and never imitate a named artist. Named references may be used only for high-level meaning, atmosphere, craft, or cultural context. Return JSON only with: creative_center string; emotional_momentum object containing arc array of strings, transition_notes string, pressure_release string; culture_lanes array of objects reference_lane, symbolic_value, usage_rule; council array of objects agent and direction; hook_directions array of strings; pocket_plan string; imagery array of strings; starter_material array of original lines; recording_notes array of strings; authenticity_checks array of strings.`,input:compact(body)});res.status(200).json({ok:true,result})}catch(e){fail(res,e)}}
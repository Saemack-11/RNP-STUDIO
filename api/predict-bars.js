import {allow,generateJson,fail,compact} from "./_lib/openai.js";
export default async function handler(req,res){if(!allow(req,res))return;try{const body=req.body||{};if(body.task==="council"){const council=await generateJson({instructions:`You are a compact songwriting council. Protect truth and originality. Return JSON only with an object containing strategist, authenticity_guard, pocket_reader, culture_editor, performance_coach; each value must be one concise actionable sentence.`,input:compact(body)});return res.status(200).json({ok:true,council})}const data=await generateJson({instructions:`You are RNP Adaptive Predictive Lyrics. Suggest options, never overwrite the seed. Never invent facts. Do not imitate named artists. Return JSON only with pocket_summary string and suggestions array of 4 objects. Each suggestion requires: bar string, transition string, rhyme string, syllables integer, timing string, beat_fit integer 0-100, authenticity integer 0-100, meaning integer 0-100, originality integer 0-100, start_seconds number or null. Keep the bars concise, original, natural, and aligned to the supplied continuation mode.`,input:compact(body)});res.status(200).json({ok:true,...data})}catch(e){fail(res,e)}}